// Update this file to set the API key shown on the landing page.
// No build step is required—this script is read directly by index.html.
window.SOLAR_MAPS_CONFIG = {
  apiKey: 'set-your-api-key-here'
};
